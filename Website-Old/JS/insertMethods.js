//Function that will insert a user on Data Base 
function insertUser() {
    var userID = document.getElementById("userID").value;
    var name = document.getElementById("name").value;
    var email = document.getElementById("email").value;
    var password = document.getElementById("password").value;
    var phone = document.getElementById("phone").value;
    var adress = document.getElementById("adress").value;
    var description = document.getElementById("description").value;
    //var photo = document.getElementById("photo").value;                      COMO PASSO A FOTO  ??

    // PASSAR OS DADOS PARA A API (FUNÇÃO QUE INSERE USER NA DB)

}

//Function that will insert a seller on Data Base 
function insertSeller() {
    var userID = document.getElementById("userID").value;
    var sellerID = document.getElementById("sellerID").value;

    // PASSAR OS DADOS PARA A API (FUNÇÃO QUE INSERE SELLER NA DB)
}

//Function that will insert a product on Data Base
function insertProduct() {
    var productID = document.getElementById("productID").value;
    var typeproduct = document.getElementById("typeproduct").value;
    var name = document.getElementById("name").value;
    var price = document.getElementById("price").value;
    var description = document.getElementById("description").value;

    // PASSAR OS DADOS PARA A API (FUNÇÃO QUE INSERE Produto NA DB)
}